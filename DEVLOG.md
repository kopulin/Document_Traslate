# DEVLOG — Document_Traslate

接手時先讀最新一則（由上往下新→舊）。

---

## 2026-08-29（第七場）— P2 架站完成，VitePress 網站可 build 可預覽

由 Claude Code 執行（使用者裁示「go，不用問」）。

### 完成項目

| 項目 | 結果 |
|------|------|
| 搬檔 | 22 份逐章 `.md`（ATP_4-02.11/ → `docs/atp-4-02.11/`），改 slug 檔名（對照表照 AGENTS.md），每檔加 frontmatter（`title`/`order`/`status`）。原 `ATP_4-02.11/*_Ch*_zh-TW.md` 已 `git rm`，PDF 留原地 |
| 圖片路徑 | `../docs/public/images/atp-4-02.11/` → `/images/atp-4-02.11/`（109 處，一次 replace）。build 後實測 base 前綴自動補為 `/Document_Traslate/images/…`，32 張圖的 Ch13 頁面全部載入 |
| 警告框 | 80 處 `> ### ⚠️/❗` 多行 blockquote → VitePress container：WARNING→`::: danger`、CAUTION/IMPORTANT→`::: warning`，帶原標題。`> **註**：…` 維持 blockquote |
| 站台程式 | `docs/.vitepress/config.ts`（zh-TW、`base: /Document_Traslate/`、四段側欄 PART ONE~THREE+附錄、local 中文搜尋、cleanUrls）、`docs/index.md`（hero + 22 卡片 + 免責橫幅 + 關於本站）、`docs/errata.md`（28 處原文勘誤，從六則 DEVLOG 彙整）、`theme/{index.ts,custom.css}`（沿用 tccc-notes IBM Carbon + 追加列印 CSS 與 figure/blockquote 樣式） |
| 部署設定 | `.github/workflows/deploy.yml`（沿用 tccc-notes）、`package.json`（vitepress ^1.6.3，實裝 1.6.4）、`.claude/launch.json`（port 5174）、`.gitignore` 併入 `node_modules/`、`docs/.vitepress/{dist,cache}` |

### 驗收

- `npm run docs:build` 通過，**無 dead link**；`dist/` 產出 24 個 HTML + 110 個 image 檔（109 圖 + `_manifest.json`）
- 本機 `vitepress dev` 預覽：首頁（hero/橫幅/22 卡片/footer 授權）、Ch06（`::: danger`/`::: warning` 樣式正確）、Ch13（32 張圖全載入、outline、側欄 active）、errata（`::: warning` callout + 表格）全部正常
- 修掉 3 處 Markdown 粗體 render bug：`**…）**字`（closing `**` 夾在全形右括號與 CJK 之間，CommonMark flanking 規則不成立）→ 改用 `<strong>`。位置：`06-respiration-ventilation.md:56`、`07-circulation.md:25,164`。其餘 `字**（英文）` 這類標準寫法 markdown-it 處理正常，已抽查確認

### 未做 / 交還使用者

- **GitHub Pages 尚未啟用**：需在 repo Settings → Pages 設 Source = GitHub Actions，push 後 `deploy.yml` 會自動 build。首次要人工按一下
- **P3｜合併全書術語表**（22 檔末合計約 530 條，未去重）
- **P3｜譯文逐章內容驗收**（機器驗證都過，內容層面仍未經使用者確認）
- errata.md 是從 DEVLOG 表格轉錄，未再逐條回原文複查

### 給下一個 agent 的提醒

1. canonical 位置是 `docs/atp-4-02.11/`，不要再碰舊路徑。改譯文直接改 `docs/` 內的檔
2. 本機預覽：`npm run docs:dev`（port 5174）。改 `config.ts` 側欄要同步 `index.md` 卡片
3. 圖片 `src` 一律 `/images/atp-4-02.11/…`，**不要**寫 `/Document_Traslate/…`，base 由 VitePress 自動加
4. 新增章節頁面時記得補 frontmatter，否則 `order` 排序會亂

---

## 2026-08-29（第六場）— P1 圖片工序完成，109 張全數入稿

### 完成項目

| 項目 | 結果 |
|------|------|
| 抽圖 | **109 張**，200 dpi PNG，存於 `docs/public/images/atp-4-02.11/` |
| 命名 | `fig-<章2位>-<序2位>.png`（附錄 `fig-a-01`／`fig-b-01`），全數合規 |
| 佔位替換 | 15 個檔、**109 處**全部轉為 `<figure>` 區塊 |
| 總大小 | 約 27 MB |

抽圖方法沿用第一場選定的**方案 B**（PyMuPDF 依版面座標重新渲染），腳本經三輪迭代才定稿，詳見下節。

### 驗收（AGENTS.md〈圖片處理工序規格〉第 3 節全數通過）

- `grep -rn '［圖' ATP_4-02.11/*.md` → **0**
- `<figure>` 109 個 ＝ `<img>` 109 條 ＝ 磁碟圖檔 109 張；**無失效連結、無未引用圖檔、無重複引用**
- `<figure>`／`</figure>`／`<figcaption>` 標籤數三者相等（每個檔案分別檢查）
- `alt` 與 `<figcaption>` 中文圖說 **100% 一致**
- 圖說頁碼與 manifest 的 `printed` 欄位 **100% 一致**
- 22 份譯稿段號連續性複驗：編輯後仍全部無缺漏、無重複
- 目視抽查 11 張（4-1／5-2／5-3／6-2／11-1／13-1／13-32／16-1／16-3／19-2／14-4）內容與圖說相符

---

### 抽圖腳本的三輪迭代（給之後要重跑的人）

| 版本 | 問題 | 修正 |
|------|------|------|
| v1 | 並排圖（4-1/4-2、4-3/4-4、5-3/5-4、13-1/13-2）只抓到第一張 | 圖說改用 **line 層級**偵測並依 y 分列，同列再依 x 中心指派圖形元素 |
| v2 | **向量表格類圖漏裁**（圖 11-1 只截到中間 190px）——因為表格文字是 text 不是 drawing，不在圖形元素聯集內 | 加 `grow()`：把與圖形區塊相交的文字併入。但矯枉過正，圖 6-2 把上方正文段落也吃進去了 |
| v3 | 同上 | `grow()` 加排除條件：**段號開頭（`\d+-\d+.`）、Note./WARNING/CAUTION、圖說行**一律不併；整行寬文字若只是擦邊（縱向重疊 < 50%）也不併 |
| v4 | 部分圖把**英文圖說一起框進圖裡**（與 `<figcaption>` 重複） | 裁切下緣強制切在圖說 y0 之上 |

v4 對 v1 的淨變化：**3 張變大**（5-5／11-1／16-3，補回漏裁）、**15 張變小**（切掉重複的英文圖說）、91 張不變。

> ⚠️ **腳本未保留**（使用者裁示不放進 repo）。原檔在裝置 session 暫存區 `$HOME/atpwork/extract2.py`，**該 session 結束即消失**。
> 圖片已全數抽出並入稿，正常情況不需要重跑；**若日後要重抽（換 DPI、改裁切、原書改版），依下列演算法重建即可**：

**演算法（PyMuPDF，逐頁處理 PDF 第 15 頁起）**

1. **找圖說**：走訪 `page.get_text('dict')` 的每個 **line**（不是 block —— 並排圖的兩個圖說會擠在同一個 block 裡），比對 `^\s*Figure\s+([0-9AB]+-\d+)\.`。依 `(y0, x0)` 排序。
2. **分列**：`y0` 相差 ≤ 6pt 視為同一列（＝並排圖）。
3. **收圖形元素**：`get_image_rects()` 的點陣影像 ＋ `get_drawings()` 的向量矩形。濾掉：面積 > 頁面 80%（整頁底框）、`h<3 且 w>400`（頁眉頁尾橫線）、`w<3 且 h>600`（整頁直線）。裁進內文範圍 `(30, 44)–(585, 740)`。
4. **指派**：某列圖說之上的帶狀區域（上界＝前一列圖說底部或內文頂緣，下界＝本列圖說頂緣）內，元素依**中心 x** 指派給最接近的圖說。
5. **併入圖內文字**（關鍵，處理向量表格類圖）：迭代把與目前裁切框（外擴 6pt）相交的文字 block 併入，直到不再變動。**排除**：圖說行、`^\s*(\d+-\d+\.\s|Note\.|WARNING|CAUTION|IMPORTANT)` 開頭的正文段落、以及寬度 > 400 但縱向重疊 < 50% 的擦邊整行文字。
6. **裁切**：外擴 4pt，再**強制把下緣切在圖說 y0 之上**（避免把英文圖說燒進圖裡，`<figcaption>` 已有）。寬或高 < 20pt 時退回整條帶狀區域。
7. **輸出**：`get_pixmap(matrix=Matrix(200/72, 200/72), clip=…, alpha=False)`，存 `fig-<章2位>-<序2位>.png`（附錄章號用小寫字母）。
8. **印刷頁碼**：`PDF 頁 − 14`，**但附錄 B 為 −13**（原書排版偏移，見第四場記錄）。務必用原文自身的 `figure X-Y on page N` 交叉引用（全書 36 處）做機器複驗。

---

### 設計師決策記錄

#### 7. 圖片擺放位置與連結寫法

| 欄位 | 內容 |
|------|------|
| 考慮選項 | A：放最終位置 `docs/public/images/atp-4-02.11/`＋相對路徑／B：放最終位置＋VitePress 絕對路徑 `/images/…`／C：暫留 `ATP_4-02.11/images/` |
| 決定 | **方案 A** |
| 理由 | 圖檔一次到位、架站時不用再搬；相對路徑讓**本機 Markdown 預覽現在就看得到圖**，方便先驗收內容 |
| 注意 | **這與 AGENTS.md 規格原本寫的「絕對路徑」不同** —— 規格寫於 10:09，使用者的對話裁示在其後，以對話為準。AGENTS.md 規則 5 已改寫，並註明架站時要轉絕對路徑 |
| 記錄時間 | 2026-08-29 |

#### 8. `_圖片測試/` 清理

已取得裝置刪除授權，**整個 `_圖片測試/`（18 個檔）已刪除**。方案 B 早已驗證確認，測試檔無保留價值。

---

### 原文本身的錯誤（本次新增 2 處）

抽圖時把每張圖的頁碼與**原文自身的交叉引用**（`figure X-Y on page N`，全書 36 處）逐一機器比對，抓到：

| 位置 | 原文問題 | 處理 |
|------|---------|------|
| 19-25 | 寫「NAPP tablet set (**figure 19-2** on page 194)」，但**圖 19-2 是 CANA、在 p.197**；p.194 上的 NAPP 錠劑組實為**圖 19-1**。誤植的是**圖號**，頁碼 194 正確 | 既有譯稿曾逕行改成「圖 19-1」（違反「不默默修正」原則），**本次已補上譯註**說明原文原貌 |
| 附錄 B 版面 | 附錄 B 的印刷頁偏移是 **PDF−13**，與全書的 −14 不同（與第四場記錄的「頁尾誤印 221」是同一個排版問題）。若照全書公式會把 B-1/B-2/B-3 標成 223/224/225 | 依原文交叉引用（B-2→p.225、B-3→p.226）校正為 **224／225／226**，manifest 加 `printed` 欄位記錄 |

其餘 34 處交叉引用與抽圖結果**完全相符**，反向印證了頁碼換算與抽圖定位都正確。

**全書原文問題累計：28 處**（第一場 6 + 第二場 4 + 第三場 5 + 第四場 11 + 本次 2）。

---

### 下一步 To-Do

- [ ] **P2｜架設 VitePress 網站**（唯一剩下的階段，規格見 AGENTS.md〈發佈架構〉）
      - 搬檔改 slug（對照表已在 AGENTS.md）、補 frontmatter
      - **圖片 `src` 相對路徑 → 絕對路徑 `/images/atp-4-02.11/…`**（一行 sed，共 109 處）
      - 警語 `> ### ⚠️ …` → VitePress container（`::: danger` / `::: warning` / `::: info`）
      - 沿用 `/Projects/tccc-notes/` 的 `custom.css`；`base` 設 `/Document_Traslate/`
- [ ] **P3｜合併全書術語表**（22 個檔案末合計約 530 條）
- [ ] **P3｜整理「原文勘誤」獨立檔**（28 處散在六則 DEVLOG，建議做成 `docs/errata.md` 隨站發佈）
- [ ] **P3｜譯文逐章驗收**（機器驗證都過了，內容層面仍未經使用者確認）

---

### 已知問題 / 技術債

- [x] ~~全書圖片未抽取~~｜已完成，109 張全數入稿
- [x] ~~`_圖片測試/` 待清理~~｜已刪除
- [ ] **VitePress 網站未架設**｜Blocking：**是**（交付前必做）
- [ ] **圖片 src 為過渡期相對路徑**｜架站時必須轉絕對路徑，否則網站上圖片全掛｜Blocking：**是**（已列入 P2 步驟）
- [ ] **譯文尚未逐章驗收**｜Blocking：否
- [ ] **各章術語表未合併**｜Blocking：否

---

### 交接狀態（2026-08-29 收尾）

**P2 架站交由 Claude Code 處理**（使用者裁示）。交接時的 repo 狀態：

- **全部變更未提交**：15 改動、6 新增、12 刪除（＝清掉的 `_圖片測試/` 追蹤檔），共約 27 MB 新增圖檔。
  使用者裁示**本 session 不 commit**，由接手方自行決定提交粒度與時機。
- 工作區內容已通過完整驗收（見上方〈驗收〉），**檔案本身是可信的**，只是尚未進版控。
- 抽圖腳本未保留，理由與重建方式見上節。

---

### 環境異動

- **裝置端安裝 `pymupdf 1.28.2`**（`python3 -m pip install --user pymupdf`）——本機原本沒有（第一場記錄的安裝是在舊機 `/Users/ux/`）。
- 取得本 repo 的**檔案刪除授權**（本 session 有效），用於清除 `_圖片測試/`。
- 新增目錄 `docs/public/images/atp-4-02.11/`（VitePress 圖片 canonical 位置，架站時直接沿用）。
- 暫存檔（`ch01.txt`／`ch02.txt`／`ch03.txt`／`appx.txt`／`extract*.py`／`v1`–`v4`）在 `$HOME/atpwork/`，**不在** repo 內。

---

### 給下一個 agent 的提醒

0. **先看 git 狀態再動手**：本 session 的成果（22 份譯稿 ＋ 109 張圖）**全部未提交**。建議先確認工作區內容無誤後自行 commit，取得一個可回滾的基準點，再開始搬檔。
1. **翻譯與圖片都完成了，接手時唯一要做的是架站（P2）。** 規格在 AGENTS.md〈發佈架構〉，slug 對照表也在那。
2. **搬檔後第一件事是改圖片路徑**：`../docs/public/images/atp-4-02.11/` → `/images/atp-4-02.11/`，109 處，一行 sed。忘了改網站上圖片會全掛。
3. `docs/public/images/atp-4-02.11/_manifest.json` 有每張圖的 `fid`／`page`（PDF 頁）／`printed`（印刷頁）／尺寸，架站要做圖片索引或對照表時直接拿來用。
4. **原文錯誤不默默修正**，一律加譯註並回填本 DEVLOG 錯誤表。第 19-25 段是前人默默修正被抓出來的案例。
5. 使用者偏好：先給粗略框架再逐項討論；不預設他懂軍事醫療背景知識；**一次只推進一個階段，做完等驗收**（CLAUDE.md 工作規則 6）。

---

## 2026-08-29（第五場）— 發佈架構定案：單一 repo ＋ VitePress

### 決策（使用者拍板）

- **決策 1 → B：單一 repo。** 網站與翻譯內容放同一個 repo，比照 [`/Projects/tccc-notes/`](../tccc-notes/) 的 VitePress 架構。舊「發佈拆分／另開 repo」規則作廢；`CLAUDE.md`、`AGENTS.md` 已同步更新。
- **決策 2 → 採用建議：** 逐章 `.md` 搬入 `docs/atp-4-02.11/`，改用英文 slug 檔名（對照表見 `AGENTS.md`〈發佈架構〉）。
- **加值項目全數採用：** 原書頁碼錨點、列印／PDF 樣式、首頁版本免責橫幅、來源授權註記（ATP 為美軍公開刊物／公有領域）。

### 參考基準

`/Projects/tccc-notes/`：VitePress 1.6、IBM Carbon 主題（`docs/.vitepress/theme/custom.css`）、
local 中文搜尋、GitHub Actions 部署、`deploy-to-github-pages` skill。本專案沿用同一套。

### 執行順序

1. **（本場，已完成）** 把決策寫進 `CLAUDE.md`／`AGENTS.md`／`DEVLOG.md`。
2. **P1｜圖片工序**（下一棒 agent）：規格見 `AGENTS.md`〈圖片處理工序規格〉。抽圖 → `docs/public/images/atp-4-02.11/`，109 處佔位改 `<figure>` 語法。**只做圖片，不搬檔、不架站。**
3. **P2｜架站**（再下一棒）：搬檔改 slug、加 frontmatter、`WARNING → ::: callout`、`config.ts` 側欄（20 章＋附錄 A/B）、`index.md`、主題、`deploy.yml`、`package.json`、`.gitignore`（加 `node_modules/`、`docs/.vitepress/dist`、`docs/.vitepress/cache`）、列印 CSS、版本橫幅。
4. **P3｜術語表、逐章驗收**（沿用下方舊 To-Do）。

### 舊 To-Do 併入說明

下方 2026-08-28 場次的「P1｜全書圖片抽取」由本場〈執行順序 2〉取代——
輸出位置與命名改以 `AGENTS.md` 新規格為準：
`docs/public/images/atp-4-02.11/fig-<章>-<序>.png`（非舊註記的 `ATP_4-02.11/images/fig_<章>_<序>.png`）。

---

## 2026-08-28（第四場）— 附錄 A、B 完成，**全書文字翻譯收尾**

### 完成項目

| 附錄 | 檔案 | 段落 | 圖 | 表 | 字數 |
|------|------|------|----|----|------|
| A | `ATP_4-02.11_App-A_急救包與核定醫材清單_zh-TW.md` | A-1～A-16 | 2 | 5 | 約 1.3 萬 |
| B | `ATP_4-02.11_App-B_救援裝備_zh-TW.md` | B-1～B-5 | 3 | – | 約 0.4 萬 |

**至此 ATP 4-02.11 的 20 章正文 + 2 個附錄全部翻譯完成，全書累計約 27.0 萬字。**
**剩餘唯一必辦工序＝全書圖片抽取（約 110 張）。**

- 表 A-1～A-5 **五張全部譯為 Markdown 表格**，含各表下方的圖例（Legend）。
- **NSN（國家庫存編號）、UAC（單位裝配碼）、零件編號一律照錄原文數字**，未做任何更動——
  這些是請領用的識別碼，動到就會出事。
- 附錄 B 的英制單位依規則 3 加註公制：29 磅 15 盎司（約 13.6 公斤）、17 磅（約 7.7 公斤）、
  6 英寸（約 15 公分）、4.5 英寸 × 4.1 碼（約 11.4 公分 × 3.7 公尺）、7.25 英寸（約 18.4 公分）等。
  **French（導管外徑規格）非英制單位，保留原文不換算。**

### 驗證結果

| 項目 | 結果 |
|------|------|
| 段號連續性 | A-1～A-16、B-1～B-5 全部連續、無缺漏無重複 |
| 表格數 | 譯文 5 張＝原文 5 張 |
| 圖片佔位數 | 附錄 A 2 張、附錄 B 3 張＝原文一致 |
| B-2 裝備清單項目數 | 譯文 16 項＝原文 16 項 |
| 正文引用回查 | **Ch03 的 5 處引用全部核對正確**：3-23／3-27／3-102／3-105 指向附錄 A 整體；**3-47 指向的「A-7 段」確為陸軍 CLS 背包**，方向無誤 |
| 用字掃描 | 無簡體字、無中國區域用詞 |

---

### ⚠️ 附錄 A 原文品質偏低（重複與誤植共 8 處）

附錄 A 是全書原文品質最差的一節，**大量段落被重複貼上**。譯文一律照錄，未刪併：

| # | 位置 | 問題 |
|---|------|------|
| 1 | 表 A-1 圖例 | 「pr* pair」與註腳「Not provided in the Marines JFAK」被排版擠在一起；且「pr」在表中未實際出現 |
| 2 | A-4 | 「Every **Solder** must be trained」——Soldier 誤植 |
| 3 | A-5 | 位於第一代小節卻寫「此一 IFAK 含兩條 CAT」，與表 A-2（1 條）矛盾，實指第二代 |
| 4 | 表 A-4 | 「胸封敷料」重複兩列；「病患檢查手套」以兩種寫法分列兩行、數量皆 4 |
| 5 | A.5 節標題 | 標題主體「FIRST AID CASE AND KITS, AUTHORIZED MEDICAL ALLOWANCE LIST FOR」被重複貼上一次 |
| 6 | A-8 | **整段內容在同一段內重複兩次**（第二次結尾漏句點） |
| 7 | A-9～A-11／A-12～A-14 | **三段完整重複**；A-9 寫「Soldiers」、A-12 寫「Sailors」，前者疑為誤植（陸戰隊與海軍配對） |
| 8 | A-15／A-16 | 兩段重複；A-16 把表格標題寫成段落正文；A-15 有「eh」（the）誤植；**陸戰隊表沿用陸軍的 UAC 245C** |
| 附 | 表 A-5 | 「減壓針 14GA」同格重複兩次；「45×45×63」燒傷敷料**缺單位** |

附錄 B 另有 3 處：頁尾頁碼印為 221（實為 224）、頁眉印為「Introduction」（應為 Appendix B）、
B-2 清單「One **role** multipurpose tape」為 roll 誤植。

**全書原文問題累計：26 處**（第一場 6 + 第二場 4 + 第三場 5 + 本次 11）。

---

### 下一步 To-Do

- [ ] **P1｜全書圖片抽取**（方案 B：PyMuPDF 依版面座標重新渲染，200 dpi PNG，約 110 張）
      —— 全書 22 個檔案的文字佔位統一改為 Markdown 圖片語法並補原書頁碼
      —— 佔位格式固定為 `> **［圖 X-X．中文圖說］**　原文圖說：Figure X-X. ...`，可用正規式批次替換
      —— 圖片檔建議放 `ATP_4-02.11/images/`，命名 `fig_<章>_<序>.png`
- [ ] **P2｜合併全書術語表**（22 個檔案末合計約 530 條）
      —— 建議骨幹順序：**Ch01（法定定義／權責）→ Ch03（TCCC 三階段／Tier／MARCH-PAWS）→ 各處置章 → 附錄 A（裝備品名）**
- [ ] **P2｜譯文逐章驗收**（目前僅跑過機器驗證，內容層面尚未經使用者確認）
- [ ] **P3｜清理 `_圖片測試/`**（12 個測試檔，方案已確認）
- [ ] **P3｜整理「原文問題總表」**（26 處散在四則 DEVLOG，可獨立成一份 `原文勘誤.md` 供交付時附上）

---

### 已知問題 / 技術債

- [x] ~~Ch01–Ch03 未翻譯~~｜已完成
- [x] ~~附錄 A、B 未翻譯~~｜已完成
- [ ] **全書圖片未抽取**｜約 110 張，22 個檔案仍為文字佔位｜Blocking：**是**（交付前必補，且為唯一剩餘 blocker）
- [ ] **譯文尚未逐章驗收**｜22 份都跑過機器驗證，內容層面未經使用者確認｜Blocking：否
- [ ] **各章術語表未合併**｜約 530 條散在 22 個檔案末，未去重｜Blocking：否
- [ ] **附錄檔名排序**｜`App-` 在檔案總管中排在 `Ch` 之前，與閱讀順序相反｜Blocking：否（已知取捨，記於 AGENTS.md）

---

### 環境異動

- 本次 session 全程未安裝任何套件；抽原文使用裝置既有的 `pdftotext -layout`。
- **下一步的圖片抽取需要 `pymupdf`**——裝置端先前已裝過 `pymupdf 1.28.2`（見第一則 DEVLOG），
  接手時先 `python3 -c "import fitz; print(fitz.__doc__)"` 確認仍在。
- 中繼檔（`ch01.txt`／`ch02.txt`／`ch03.txt`／`appx.txt`）在裝置 session 暫存區 `$HOME/atpwork/`，**不在** repo 內。

---

### 給下一個 agent 的提醒

1. **文字已全部翻完，接手時唯一要做的是圖片抽取。** 這是全書 22 個檔案唯一還會被動到的工序，
   建議寫一支腳本一次處理完，不要逐檔手改。
2. 抽原文指令：`pdftotext -layout -f <起> -l <迄> <PDF> <輸出>`，**PDF 頁碼＝印刷頁碼 + 14**。
3. 結構慣例（22 份一致）：檔頭譯註區塊 → `## X.n` / `### X.n.m` 標題 → 段號 `**X-N.**`
   → 警語 `> ### ⚠️ 警告（WARNING）` / `> ### ⚠️ 注意（CAUTION）` → 圖片文字佔位
   → 章末〈本章術語對照表〉。**操作步驟一律用編號清單**。
4. **原文錯誤不默默修正**，一律加譯註標明，並回填至本 DEVLOG 的錯誤表。
   附錄 A 是重災區（8 處重複／誤植），已全部標註完畢，不需再回頭處理。
5. 驗證流程（每份必跑）：段號連續性比對 `seq`、警語／圖／表數量與原文比對、關鍵數值逐項核對、
   簡體字與陸用詞掃描。
6. 使用者偏好：先給粗略框架再逐項討論；不預設他懂軍事醫療背景知識。

---

## 2026-08-28（第三場）— 補譯 Ch02、Ch01，**正文 20 章全數完成**

### 完成項目

| 章 | 檔案 | 段落 | 圖 | 表 | 字數 |
|----|------|------|----|----|------|
| Ch02 | `ATP_4-02.11_Ch02_傷患脫離與搬運_zh-TW.md` | 2-1～2-17 | 無 | 無 | 約 0.6 萬 |
| Ch01 | `ATP_4-02.11_Ch01_傷患處置_zh-TW.md` | 1-1～1-111 | 4 | 1 | 約 3.1 萬 |

**至此 ATP 4-02.11 的 Ch01–Ch20 正文全部翻譯完成，全書累計約 25.3 萬字。**

- Ch01 檔頭一併譯出 **PART ONE（傷患處置）的部序言**（Ch03 已譯出 PART TWO 部序言）。
- **表 1-1〈醫療層級職責〉已完整譯為 Markdown 表格**（四個 Tier × CUF／TFC 兩階段，
  加上 TACEVAC 階段的共通職責）——這是全書第一張跨層級職責對照表。
- 圖 1-1～1-4 以文字佔位保留；**圖 1-4 的完整圖例（AH／CRTS／FEBA／GCE／LCE／LHA／LHD／LZ）已一併譯出**，
  抽圖時可直接沿用。
- Ch01 建立了全書的**術語法定定義**：casualty vs. patient 的身分切換點、CCP／BAS／MTF 的層級關係、
  CASEVAC vs. MEDEVAC 的關鍵區別（有無途中照護）。**續作附錄時請以 Ch01 術語表為準。**

### 驗證結果

| 章 | 段號連續性 | 警語／圖表數 |
|----|-----------|-------------|
| Ch02 | 2-1～2-17 連續、無缺漏無重複 | WARNING 2、Note 2（與原文一致）；無圖表 |
| Ch01 | 1-1～1-111 連續、無缺漏無重複 | WARNING 1、Note 3、Figure 4、Table 1（**全部與原文逐項相符**） |

關鍵數值核對無誤：後送優先序（Priority I／IA 一小時、II 四小時、III 24 小時、IV 便利）、
CLS 認證頻率（陸軍每班至少 1 名、每年複訓；陸戰隊每名編制醫務兵至少訓練 2 名陸戰隊員）、
連資深醫務兵位置（連後方 500 至 1000 公尺）、徒手搬運 50–300 公尺、拖行 50 公尺以內。
Ch01、Ch02 的距離單位原文即為公制，**未做換算**。

---

### 原文本身的錯誤（本次新增 5 處，已加譯註）

| 位置 | 原文問題 |
|------|---------|
| 1-5 | 稱 CoTCCC「由陸軍與空軍的代表組成」，未列海軍與陸戰隊，與全書聯合三軍架構不符，推測漏列 |
| 1-84 | 指向「本準則第 20 章」查閱污染傷患照護，但第 20 章為〈戰鬥與作戰壓力控制〉，**應為第 19 章** |
| 圖 1-3 圖說 | 「Taxonomy **Contiumim** of Health Care **Capabilitles**」——兩處拼字誤植（Continuum／Capabilities） |
| 2-17 | 把 STP 稱為「**Standard** Training Publication」，但 2-1 段全稱為「**Soldier** Training Publication」 |
| 2-17 | 修訂版編號寫為「STP 8-68W13-**SG**-TG」，2-1／2-16 段則為「-**SM**-TG」，中段代碼不一致 |

另有一處交叉引用不精確（非錯誤，已於 Ch02 加註）：
Ch03 的 3-43 段以「見第 2 章 2-6 段」指引搬運技術，該段實為後送權責，**技術本身在 2-13／2-14**。

**全書原文錯誤累計：15 處**（第一場 6 處 + 第二場 4 處 + 本次 5 處）。

---

### 下一步 To-Do

- [ ] **P1｜翻譯附錄 A**（急救包內容清單，印刷頁 217–223／PDF 頁 231–237）
      —— 5 個表格，含圖 A-1／A-2。**全書共 7 處引用附錄 A**：
         Ch01（1-…無）、Ch03 的 3-23／3-27／3-47（A-7 段）／3-102／3-105。翻譯時逐一核對段號。
- [ ] **P1｜翻譯附錄 B**（救援裝備，印刷頁 224–226／PDF 頁 238–240，圖 B-1～B-3）
- [ ] **P2｜全書圖片抽取**（方案 B，約 110 張），並把 20 章譯文中的文字佔位統一改為 Markdown 圖片語法
      —— 這是目前唯一還會動到既有 20 章檔案的工序
- [ ] **P2｜合併全書術語表**（各章末合計約 480 條）
      —— 建議骨幹順序：**Ch01（法定定義／權責）→ Ch03（TCCC 三階段／Tier／MARCH-PAWS）→ 各處置章**
- [ ] **P3｜譯文逐章驗收**（目前只跑過機器驗證：段號、警語數、關鍵數值；內容層面尚未經使用者確認）
- [ ] **P3｜清理 `_圖片測試/`**（12 個測試檔，方案已確認）

---

### 已知問題 / 技術債

- [x] ~~Ch01、Ch02、Ch03 未翻譯~~｜**正文 20 章已全數完成**
- [ ] **附錄 A、B 未翻譯**｜影響：全書完整性，且正文多處引用附錄 A｜Blocking：**是**（交付前必補）
- [ ] **全書圖片未抽取**｜約 110 張，譯文仍為文字佔位｜Blocking：**是**（交付前必補）
- [ ] **譯文尚未逐章驗收**｜20 章都跑過機器驗證，但內容層面未經使用者確認｜Blocking：否
- [ ] **各章術語表未合併**｜約 480 條散在 20 章末，未去重｜Blocking：否

---

### 環境異動

- 本次 session 未安裝任何套件；抽原文使用裝置既有的 `pdftotext -layout`。
- 中繼檔（`ch01.txt`／`ch02.txt`／`ch03.txt`）在裝置的 session 暫存區 `$HOME/atpwork/`，**不在** repo 內。

---

### 給下一個 agent 的提醒

1. **正文已全部完成，接手時的第一件事是附錄 A、B。** 附錄 A 是 5 個表格為主的裝備清單，
   翻譯時務必回頭核對 Ch03 的 5 處引用段號（特別是 3-47 引用的「A-7 段」）。
2. 抽原文指令：`pdftotext -layout -f <起> -l <迄> <PDF> <輸出>`，**PDF 頁碼＝印刷頁碼 + 14**。
3. 結構慣例（20 章一致）：檔頭譯註區塊 → `## X.n` / `### X.n.m` 標題 → 段號 `**X-N.**`
   → 警語 `> ### ⚠️ 警告（WARNING）` / `> ### ⚠️ 注意（CAUTION）` → 圖片文字佔位
   → 章末〈本章術語對照表〉。**操作步驟一律用編號清單**（原文多為項目符號）。
4. **原文錯誤不默默修正**，一律加譯註標明，並回填至本 DEVLOG 的錯誤表。
5. 驗證流程（每章必跑）：段號連續性比對 `seq`、警語／圖／表數量與原文比對、關鍵數值逐項核對、
   簡體字與陸用詞掃描。
6. 使用者偏好：先給粗略框架再逐項討論；不預設他懂軍事醫療背景知識。

---

## 2026-08-28（第二場）— 補譯 Ch03，並裁示 AGENTS.md 規則衝突

> **本次 session 在雲端執行，連線到 `pumpkin-m1-prodemacbook-pro-local` 的
> `/Users/pumpkin/Documents/Projects/Document_Traslate`**（前一則 DEVLOG 記錄的路徑為
> `/Users/ux/...`，係使用者換機所致，同一份 repo）。

### 完成項目

| 章 | 檔案 | 段落 | 圖 | 表 |
|----|------|------|----|----|
| Ch03 | `ATP_4-02.11_Ch03_TCCC基礎_zh-TW.md` | 3-1～3-114 | 無 | 無 |

- 全章 **114 段全數譯出**，約 2.4 萬字；累計全書約 **21.6 萬字**。
- 檔頭一併譯出 **PART TWO（戰術戰傷救護）的部序言**，因原書該段落緊接於 Ch03 之前。
- 結構比照 Ch04–Ch20：檔頭譯註區塊 → `## 3.x` / `### 3.x.y` 標題 → 段號 `**3-N.**` →
  警語 `> ### ⚠️ 注意（CAUTION）` → 章末〈本章術語對照表〉（本章新增 60 餘條）。
- 原文的項目符號式操作步驟（3-62 九線 MEDEVAC、3-107 CLS 背包責任）依既有慣例改為**編號清單**。

### 驗證結果

- 段號 **3-1～3-114 連續、無缺漏、無重複**（機器比對 `seq 1 114`）。
- 警語數量與原文一致：**CAUTION 1 則、Note 3 則、WARNING 0 則**。
- 關鍵數值逐項核對無誤：12 個月複訓、25 秒（九線第 1–5 線）、1–3 秒（呼叫等待）、
  一分鐘（止血帶施加）、三分鐘（失血致死）、兩小時（immediate 分類）、
  Component 1／2／3 對應每年／兩年複訓、CLS 套件處置量（一名雙肢＋兩名單肢）。
- 本章**無英制單位**需換算，**無藥品劑量**。
- 用字檢查：無簡體字、無中國區域用詞。

---

### 設計師決策記錄

#### 5. AGENTS.md 規則 3（單位換算）

| 欄位 | 內容 |
|------|------|
| 考慮選項 | A：改規則，保留現行「原數值＋括號加註公制」作法／B：照原規則，回頭清掉 17 章裡所有換算 |
| 決定 | **方案 A**，AGENTS.md 規則 3 已改寫 |
| 理由 | 與 Ch04–Ch20 既有 17 章一致，不必回頭改稿；並明確切分「藥品劑量完全不換算」與「一般度量單位加註公制」兩種處理 |
| 可重新討論的條件 | 若之後要做正式出版品且有統一體例要求，可再議 |
| 記錄時間 | 2026-08-28 |

#### 6. AGENTS.md 規則 5（圖片語法）

| 欄位 | 內容 |
|------|------|
| 考慮選項 | A：維持文字佔位，等抽圖時再全書統一轉語法／B：這一輪先抽圖，把 17 章佔位全部轉正，之後才續譯 |
| 決定 | **方案 A**，AGENTS.md 規則 5 已改寫為「兩階段處理」 |
| 理由 | 先把文字補齊，抽圖仍是一次性批次工序，晚做成本相同；優先解掉 Ch03 這個 blocking 缺口 |
| 可重新討論的條件 | 文字全部翻完（含 Ch01、Ch02、附錄 A/B）後即應執行抽圖 |
| 記錄時間 | 2026-08-28 |

---

### 原文本身的錯誤（本章新增 4 處，已加譯註）

| 位置 | 原文問題 |
|------|---------|
| 3-26 | 「Perform Casualty Burn Care (paragraph 13-9)」——13-9 講的是肌腱，燒傷照護實為 **12-9** |
| 3-26 | 「Record Treatment on a DD Form 1380 (paragraph 16-13)」——16-13 講的是蛇類分類，DD Form 1380 實為 **15-13～15-19** |
| 3-36 / 3-57 | 3-36 寫「Tactical Evacuation Care（亦稱 TACEVAC）」，3-57 卻指示不應以 TACEVAC 稱呼此階段，**前後自相矛盾** |
| 3-95 | 「Hypothermia and head injuries (see Chapter 8)」——第 8 章僅涵蓋低體溫，頭部傷勢實為 **第 11 章** |

（3-26 第 1 項「Apply Rigid Eye Shield (paragraph 10-1)」指向第 10 章眼部創傷，方向正確，未列為錯誤。）

**全書原文錯誤累計：10 處**（前一則 6 處 + 本次 4 處）。

---

### 下一步 To-Do

- [ ] **P1｜補譯 Ch01**（Casualty Response，印刷頁 1–22／PDF 頁 15–36）—— 全書最長的一章
- [ ] **P1｜補譯 Ch02**（Casualty Extraction and Movement，印刷頁 23–26／PDF 頁 37–40）
      —— Ch03 的 3-43 段引用了「第 2 章 2-6 段」，補譯時順便核對此交叉引用是否正確
- [ ] **P2｜翻譯附錄 A、B**
      —— 附錄 A：急救包內容清單（印刷頁 217–223，5 個表格，含圖 A-1／A-2）
          Ch03 有 4 處引用附錄 A（3-23、3-27、3-47 的 A-7 段、3-102、3-105），補譯時核對
      —— 附錄 B：救援裝備（印刷頁 224–226，圖 B-1～B-3）
- [ ] **P3｜全書圖片抽取**（方案 B，約 110 張），並把譯文中的文字佔位改為 Markdown 圖片語法
- [ ] **P3｜合併全書術語表**（各章末合計 360+ 條，需去重並統一；Ch03 的術語表可作為全書骨幹，
      因為它涵蓋 TCCC 三階段、Tier 分級、MARCH-PAWS、檢傷四類等全書共通概念）
- [ ] **P3｜清理 `_圖片測試/`**（12 個測試檔，方案已確認）

---

### 已知問題 / 技術債

- [ ] **Ch01、Ch02 未翻譯**｜影響：全書完整性｜Blocking：**是**（交付前必補）
- [x] ~~Ch03 未翻譯~~｜已於本次 session 完成
- [x] ~~AGENTS.md 進度表過期~~｜已補至 Ch20 + Ch03
- [x] ~~AGENTS.md 規則 3／5 與實作不一致~~｜已依使用者裁示改寫
- [ ] **譯文尚未逐章驗收**｜Ch03 已跑機器驗證（段號／警語／數值），但**內容層面仍未經使用者確認**｜Blocking：否
- [ ] **各章術語表未合併**｜跨章一致性靠 agent 比對既有章節維持，未經機器驗證｜Blocking：否

---

### 環境異動

- 本次 session 未安裝任何套件。抽取原文使用裝置既有的 `pdftotext -layout`。
- 中繼檔（`ch03.txt`）寫在裝置的 session 暫存區（`$HOME/atpwork/`，**不在** repo 內），不會污染 repo。

---

### 給下一個 agent 的提醒

1. Ch03 已完成，**續譯 Ch01／Ch02 時請以 Ch03 的術語表為準**——它是全書概念的骨幹
   （TCCC 三階段、Tier 1–4、MARCH-PAWS、檢傷四類、JFAK/IFAK、九線 MEDEVAC 等）。
2. Ch01、Ch02 屬 PART ONE，位於 Ch03 之前；補譯後**檔案排序自然接續**，不需調整命名。
3. 抽原文的指令：`pdftotext -layout -f <起> -l <迄> <PDF> <輸出>`，PDF 頁碼＝印刷頁碼 + 14。
4. **原文錯誤不默默修正**，一律加譯註標明，並回填至本 DEVLOG 的錯誤表。
5. 使用者偏好：先給粗略框架再逐項討論；不預設他懂軍事醫療背景知識；一次處理一章，翻完等驗收。

---

## 2026-08-28 — Ch04–Ch20 全數完成（雲端 session，跨裝置交接）

> **交接重點：使用者切換到另一台電腦，請從本則往下讀。**

### 完成項目

本次 session 一口氣翻完 **Ch13–Ch20 共 8 章**，加上先前的 Ch04–Ch12，
ATP 4-02.11 的 **Ch04 至 Ch20（17 章）全部完成**，累計約 **19.2 萬字**。

| 章 | 檔案 | 段落 | 圖 | 表 |
|----|------|------|----|----|
| Ch13 | `ATP_4-02.11_Ch13_骨折與固定_zh-TW.md` | 13-1～13-55 | 32 | – |
| Ch14 | `ATP_4-02.11_Ch14_其他二次傷勢的應急技術_zh-TW.md` | 14-1～14-24 | 10 | – |
| Ch15 | `ATP_4-02.11_Ch15_傷患監測與後送準備_zh-TW.md` | 15-1～15-41 | 2 | – |
| Ch16 | `ATP_4-02.11_Ch16_叮咬與螫傷_zh-TW.md` | 16-1～16-83 | 8 | – |
| Ch17 | `ATP_4-02.11_Ch17_氣候與環境傷害_zh-TW.md` | 17-1～17-55 | – | – |
| Ch18 | `ATP_4-02.11_Ch18_鐮刀型血球特徵_zh-TW.md` | 18-1～18-23 | – | – |
| Ch19 | `ATP_4-02.11_Ch19_CBRN環境急救_zh-TW.md` | 19-1～19-96 | 7 | 4 |
| Ch20 | `ATP_4-02.11_Ch20_戰鬥與作戰壓力控制_zh-TW.md` | 20-1～20-40 | – | 3 |

每章都跑過驗證：段落編號連續無缺漏、圖表編號齊全、警語（WARNING／CAUTION／IMPORTANT）
數量與原文一致、關鍵數值（劑量、溫度、時間門檻）逐項核對。

---

### ⚠️ 更正：Ch01–Ch03 尚未翻譯

**本次 session 中曾對使用者誤稱「Ch1–Ch20 正文全部翻完」，此陳述錯誤，特此更正。**

原書 PART ONE 的三章**從未翻譯**，翻譯是從 Ch04 起跳的（當初以 Ch04 作為試譯樣本，
之後便一路往下推進，未回頭補前三章）：

| 章 | 原書章名 | 印刷頁 | PDF 頁 | 狀態 |
|----|---------|--------|--------|------|
| Ch01 | Casualty Response | 1–22 | 15–36 | **未翻譯** |
| Ch02 | Casualty Extraction and Movement | 23–26 | 37–40 | **未翻譯** |
| Ch03 | Fundamentals of Tactical Combat Casualty Care | 27–42 | 41–56 | **未翻譯** |

Ch03 尤其重要——它是 TCCC 的基礎章（含 CUF／TFC／TACEVAC 三階段定義、
Tier 1／Tier 2 分級、MARCH-PAWS 全貌），**後面 17 章都在引用它的概念**。
建議補譯時優先處理 Ch03。

---

### 設計師決策記錄

#### 1. 術語譯法（Ch04 試譯後拍板，全書沿用）

| 欄位 | 內容 |
|------|------|
| 考慮選項 | hasty／deliberate TQ：A「快速式／確實式」 vs B「應急式／精確式」；high and tight：A 譯「高位緊縛」 vs B 保留原文；blood sweep：A「摸血檢查」 vs B「掃血」；Service member：A「官兵」 vs B「服役人員」 |
| 決定 | **應急式／精確式止血帶**、**high and tight 保留原文不譯**、**掃血**、**服役人員** |
| 理由 | 使用者直接指定，未展開說明 |
| 可重新討論的條件 | 未設限；若之後有國軍既有譯法可對照，可再統一 |
| 記錄時間 | 2026-08-28 |

延伸決策（agent 自行判斷、使用者未反對）：
`Soldier` 譯「**官兵**」、`Service member` 譯「**服役人員**」——原文刻意區分陸軍專指與各軍種泛指，譯文保留此區分。

#### 2. 圖片處理方案

| 欄位 | 內容 |
|------|------|
| 考慮選項 | A：原圖保留英文，圖下另補中文圖例／B：HTML 絕對定位把中文標籤疊在圖上／C：用 Figma 重繪 110 張中文版圖 |
| 決定 | **方案 A** |
| 理由 | 這是實務訓練教材，受訓者遲早要接觸英文術語，圖上保留英文反而有訓練價值；中文圖例放圖下方查閱一樣即時。B 的 RWD 會跑掉、C 工序太重 |
| 可重新討論的條件 | 若之後改為正式出版品，可重新評估 C |
| 記錄時間 | 2026-08-28 |

#### 3. 圖片抽取的技術方案（已實測驗證）

| 欄位 | 內容 |
|------|------|
| 考慮選項 | A：`pdfimages` 直接抽內嵌影像／B：PyMuPDF 讀出圖片座標後按版面範圍重新渲染 |
| 決定 | **方案 B** |
| 理由 | 實測發現全書 127 張影像中有 **52 張帶透明遮罩（smask）**，方案 A 抽出來背景會變全黑；且方案 A 會遺失疊在照片上的向量圖框與箭頭。方案 B 輸出即所見（200 dpi PNG，約 70–220 KB／張） |
| 可重新討論的條件 | 無 |
| 記錄時間 | 2026-08-28 |

測試檔在 `ATP_4-02.11/_圖片測試/`（12 個檔，A_／B_ 前綴為兩種方案對照），**方案確認後可刪**。

#### 4. 執行順序

| 欄位 | 內容 |
|------|------|
| 考慮選項 | A：邊翻文字邊處理圖片／B：文字全部翻完再統一處理圖片 |
| 決定 | **方案 B** |
| 理由 | 抽圖是一次性批次工序，現在做跟最後做成本一樣；但術語表要先穩定，中文圖例才不會前後不一致 |
| 可重新討論的條件 | 無 |
| 記錄時間 | 2026-08-28 |

---

### ⚠️ 與 AGENTS.md 既有規則的衝突（需使用者裁示）

本次 session 的實際作法與 `AGENTS.md`〈翻譯規則〉有兩處不一致，**尚未修改 AGENTS.md**：

| # | AGENTS.md 原規則 | 實際作法 | 說明 |
|---|-----------------|---------|------|
| 1 | 規則 3：「藥名、劑量、單位**完全照原文，數字與單位不換算**」 | **保留原數值＋括號加註公制換算**（如「華氏 104 度（攝氏 40 度）」「2 英寸（約 5 公分）」） | 使用者在本次 session 明確指定「英制單位：保留原數值＋加註公分」。**藥品劑量則完全未換算**，符合原規則 |
| 2 | 規則 5：圖片用 `![原圖說明](相對路徑)` 引用，並標注原書頁碼 | 用文字佔位 `> ［圖 X-X．中文圖說］　原文圖說：Figure X-X. ...` | 因圖片尚未抽出，暫以佔位標註；抽圖後需**全書統一改為 Markdown 圖片語法**並補原書頁碼 |

**建議**：使用者確認後，由下一個 agent 更新 AGENTS.md 規則 3 與 5 的文字，讓文件與實作一致。

---

### 原文本身的錯誤（已在譯文中以譯註標記，共 6 處）

2026 年 3 月版原文有數處交叉引用與內容錯誤，推測是改版時段號重編的殘留。
**譯文一律不默默修正，全部加譯註標明**：

| 位置 | 原文問題 |
|------|---------|
| 12-15 | 引用「paragraph 13-9 through 13-14」，應為本章 12-9～12-14 |
| 12-4 | 九則律百分比相加為 101%（臀部 1% 通常已含在軀幹後側 18% 內） |
| 13-12 | 引用「paragraph 14-13」，第 14 章內容對不上 |
| 13-13 | 引用「paragraph 14-10 on page 122」，應為 13-10 |
| 13-43 | 下肢骨折卻寫「check distal (at the wrist) pulse」，應為腳踝 |
| 14-6 | 引用「paragraph 15-5」，應為本章 14-5 |
| 17-23 | 句中寫「heat illness」但括號與上下文均為寒冷天候傷害 |

---

### 下一步 To-Do

- [ ] **P1｜補譯 Ch03**（Fundamentals of TCCC，印刷頁 27–42／PDF 41–56）
      —— TCCC 基礎章，後面 17 章都引用其概念，缺這章整份譯稿的前後銜接會斷掉
- [ ] **P1｜裁示 AGENTS.md 規則衝突**（單位換算、圖片語法兩項），並更新 AGENTS.md
- [ ] **P2｜補譯 Ch01、Ch02**（傷患處置／傷患脫離與搬運）
- [ ] **P2｜翻譯附錄 A、B**
      —— 附錄 A：急救包內容清單（印刷頁 217–223，5 個表格，含圖 A-1／A-2）
      —— 附錄 B：救援裝備（印刷頁 224–226，圖 B-1～B-3）
- [ ] **P2｜更新 AGENTS.md 進度表**（目前只列到 Ch12，需補 Ch13–Ch20）
- [ ] **P3｜全書圖片抽取**（方案 B，約 110 張），並把譯文中的文字佔位改為 Markdown 圖片語法
- [ ] **P3｜合併全書術語表**（目前散在各章末，共 300+ 條，需去重並統一）
- [ ] **P3｜清理 `_圖片測試/`**（12 個測試檔，方案已確認）

---

### 已知問題 / 技術債

- [ ] **Ch01–Ch03 未翻譯**｜影響：全書完整性，Ch03 尤其影響前後概念銜接｜Blocking：**是**（交付前必補）
- [ ] **AGENTS.md 進度表過期**｜只列到 Ch12，與實際（Ch04–Ch20）不符｜Blocking：否
- [ ] **AGENTS.md 規則 3／5 與實作不一致**｜見上節｜Blocking：否（但會誤導下一個 agent）
- [ ] **譯文尚未逐章驗收**｜使用者每章都說「繼續」，未做內容層面的品質確認｜Blocking：否
- [ ] **各章術語表未合併**｜跨章一致性靠 agent 記憶維持，未經機器驗證｜Blocking：否

---

### 環境異動

- 使用者裝置端安裝 `pymupdf 1.28.2`（原因：圖片抽取方案 B 需要讀取圖片版面座標）
- 新增資料夾存取授權：`/Users/ux/Documents/Projects/Document_Traslate`（原因：原本只連到 `ATP_4-02.11/` 子資料夾，寫 DEVLOG 需要父層權限）
- 未安裝其他套件；`pdftotext`／`pdfimages`／`pdftoppm` 為裝置既有工具

---

### 給下一個 agent 的提醒

1. 譯文的**結構慣例**已在 Ch04–Ch20 統一，續譯 Ch01–Ch03 時請比照：
   檔頭譯註區塊 → 章節內容（段號用 `**X-Y.**` 粗體）→ 警語用 `> ### ⚠️ 警告（WARNING）` 引用區塊
   → 圖片文字佔位 → 章末「本章新增術語對照表」
2. **操作步驟一律用編號清單**（原文多為項目符號），因為這些是有序動作且會引用「圖 X 步驟 A/B/C」。
3. **原文錯誤不默默修正**，一律加譯註標明。
4. 使用者偏好：先給粗略框架再逐項討論；不預設他懂軍事醫療背景知識。

---

## 2026-08-28 — 專案初始化

- 將資料夾轉為獨立 git repo，remote 接上 `https://github.com/kopulin/Document_Traslate.git`，並加入父層 `/Projects/.gitignore`。
- 建立 `CLAUDE.md`（路由／規則）、`AGENTS.md`（進度＋翻譯規則）、`.gitignore`、本 `DEVLOG.md`。
- 確立範圍邊界：本 repo 只維護翻譯內容（Markdown），未來 HTML 發佈另開獨立 repo。
- 現況：`ATP_4-02.11/` 已有 Ch04–Ch12 譯文、原始 PDF、圖片抽取測試（`_圖片測試/`）。這批既有譯文尚未逐章驗收。
- 待辦：確認既有章節翻譯品質、補 Ch01–Ch03（若原書有）、規劃術語表。
